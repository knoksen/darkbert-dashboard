module.exports = {
  plugins: {
    // The new Tailwind PostCSS plugin (wraps the core tailwindcss logic)
    '@tailwindcss/postcss': {},
    // Autoprefixer stays the same
    autoprefixer: {},
  },
}
